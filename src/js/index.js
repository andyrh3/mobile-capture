import _ from 'lodash';
import '../scss/styles.scss';
import CondorLogo from '../images/condor-logo.min.svg';
import TcaHoriLogo from '../images/tca-logo-hori.min.svg';
import TcaVertLogo from '../images/tca-logo-vert-neg.min.svg';

/*function component() {
  let element = document.createElement('div');
  let btn = document.createElement('button');

  // Lodash, currently included via a script, is required for this line to work
  element.innerHTML = _.join(['Hello', 'webpack'], ' ');
  element.classList.add('test');

  btn.innerHTML = 'Click me and check the console!';
  btn.onclick = printMe;
  element.appendChild(btn);

  // Add the image to our existing div.
  var condorLogo = new Image();
  condorLogo.src = CondorLogo;
  element.appendChild(condorLogo);

  return element;
}

document.body.appendChild(component());*/

document.getElementsByClassName('li.country');
